<%= form_with(model: todoitem) do |form| %>
  <% if todoitem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(todoitem.errors.count, "error") %> prohibited this todoitem from being saved:</h2>

      <ul>
        <% todoitem.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :itemDescription %>
    <%= form.text_field :itemDescription %>
  </div>

  <div class="field">
    <%= form.label :priorityNumber %>
    <%= form.number_field :priorityNumber %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
