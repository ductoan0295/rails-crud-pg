<%= form_with(model: to_do_item) do |form| %>
  <% if to_do_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(to_do_item.errors.count, "error") %> prohibited this to_do_item from being saved:</h2>

      <ul>
        <% to_do_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :itemDescription %>
    <%= form.text_field :itemDescription %>
  </div>

  <div class="field">
    <%= form.label :priorityNumber %>
    <%= form.number_field :priorityNumber %>
  </div>

  <div class="field">
    <%= form.label :User_id %>
    <%= form.text_field :User_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
